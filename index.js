const forELemrent = document.getElementById("studentForm");
const Tableref = document.getElementById("studentsTable").querySelector("tbody"); 

Tableref.addEventListener("submit", OnSubmit);

function OnSubmit(event) {
    event.preventDefault();


    const form = event.currentTarget;

    const students = {
        id: Data.now
        firstName:
        lastName:
        age:
        course:
        faculty:
            querySelector();
    }





    event.reset();

}


